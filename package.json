{
  "name": "badminton-2026",
  "version": "1.0.0",
  "description": "Live badminton scoreboard & match management (Firebase Realtime Database). Static, classic-script architecture; esbuild bundles + minifies for production without changing the runtime model.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node scripts/serve.mjs . 5173",
    "preview": "npm run build && node scripts/serve.mjs dist 4173"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arithatneti-svg/badminton-2026.git"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
